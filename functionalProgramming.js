
// version 0
const addTwoNumbers = function(x,y) {
    const sum = x + y
    return sum
}


// version 1
const addOther = (x,y) => {
    const sum = x + y
    return sum;
}

// version 2
// if the only line of the function is the return, the curly brackets and return-keyword are not necessary
const add = (x, y) => x + y


//if want to return an object

const getPersonData = () => ({
    name: 'John Doe',
    age: 34,
    job: 'programmer',
})

